@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find and declare Eigen3 as a dependency
find_dependency(Eigen3 REQUIRED)

# Include the file that defines the actual library target (transform::transform)
include("${CMAKE_CURRENT_LIST_DIR}/transform_targets.cmake")
