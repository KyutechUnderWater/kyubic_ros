repos:
-   repo: local
    hooks:
    -   id: ament-clang-format
        name: ament_clang_format
        entry: ament_clang_format --reformat
        # language: system で、source 済みの環境にあるコマンドを実行
        language: system
        # C/C++のソースファイルのみを対象にする
        files: \.(cpp|hpp|c|h)$
